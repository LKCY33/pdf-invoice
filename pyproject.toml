[project]
name = "invoice-sum"
version = "0.1.0"
description = "Extract totals from invoice PDFs and compute sums"
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
